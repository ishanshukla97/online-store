import React from "react";
import { connect } from "react-redux";
import "./index.css";

const CheckoutForm = () => {
	return <div className="form">Address Form</div>;
};

export default connect()(CheckoutForm);
